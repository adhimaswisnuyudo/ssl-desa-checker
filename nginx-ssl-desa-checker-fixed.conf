# Nginx Configuration untuk BedaSSL Checker - FIXED VERSION
# Deploy sebagai subdirectory: https://csirt.bandungkab.go.id/ssl-desa-checker

# Pastikan ini ditambahkan di dalam server block yang sudah ada
# atau buat location block baru

# Location untuk BedaSSL Checker - FIXED
location /ssl-desa-checker {
    # Gunakan alias untuk subdirectory
    alias /var/www/html/ssl-checker/public;
    
    # Index file
    index index.html;
    
    # Try files dengan fallback
    try_files $uri $uri/ /ssl-desa-checker/index.html;
    
    # Autoindex untuk debugging (hapus di production)
    autoindex on;
    
    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    
    # Cache static files
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        add_header Access-Control-Allow-Origin "*";
    }
    
    # Handle HTML files
    location ~* \.html$ {
        add_header Cache-Control "no-cache, no-store, must-revalidate";
        add_header Pragma "no-cache";
        add_header Expires "0";
    }
}

# Proxy untuk API backend - FIXED
location /ssl-desa-checker/api {
    # Remove /ssl-desa-checker prefix untuk backend
    rewrite ^/ssl-desa-checker/api/(.*) /api/$1 break;
    
    proxy_pass http://127.0.0.1:3001;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection 'upgrade';
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_cache_bypass $http_upgrade;
    
    # Timeout settings
    proxy_connect_timeout 60s;
    proxy_send_timeout 60s;
    proxy_read_timeout 60s;
    
    # Buffer settings
    proxy_buffering on;
    proxy_buffer_size 4k;
    proxy_buffers 8 4k;
}

# Health check endpoint - FIXED
location /ssl-desa-checker/health {
    proxy_pass http://127.0.0.1:3001/api/health;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
}

# Debug endpoint untuk troubleshooting
location /ssl-desa-checker/debug {
    alias /var/www/html/ssl-checker/public;
    autoindex on;
    add_header Content-Type "text/plain";
}
